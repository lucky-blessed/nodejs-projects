# GitHub User Activity CLI

A simple command-line tool that fetches and displays the recent activity of a GitHub user using the GitHub API.

## 🚀 Features
- Fetch recent public GitHub activity
- Display readable activity summaries in the terminal
- Handle invalid usernames and API errors gracefully
- Built using native Node.js APIs (no external HTTP libraries)

## 📦 Usage

```bash
github-activity <github-username>
